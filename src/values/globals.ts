// Use this if you're running the server through: npm run dev
export const host = `localhost:${5000}`;

// Use this if you're running the server through docker
// export const host = `node-server:${5000}`;